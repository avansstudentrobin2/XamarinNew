<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ServerApp"
             x:Class="ServerApp.MainPage">
    <ScrollView>
        <StackLayout Spacing="10" Padding="10">
            <Label Text="Welkom!" 
               HorizontalOptions="CenterAndExpand"
                   FontSize="Large"/>

            <Label Text="Log in om je servers te bekijken"
                   HorizontalOptions="Start"
                   FontSize="Medium"/>

            <Label Text="Gebruikersnaam:"
                   HorizontalOptions="Start"
                   FontSize="Small" />
            <Entry x:Name="Username" Placeholder="Gebruikersnaam"/>

            <Label Text="Wachtwoord:"
                   HorizontalOptions="Start"
                   FontSize="Small" />
            <Entry x:Name="Password"
                   IsPassword="True"
                   Placeholder="Wachtwoord"/>

            <Button Text="Log in" 
                    Clicked="OnLoginButtonClicked"/>

            <Button Text="Registreren"
                    Clicked="RegisterNewUserButtonClicked" />

        </StackLayout>
    </ScrollView>
</ContentPage>
